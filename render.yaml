services:
  - type: worker
    name: fn-matchmaker
    env: python
    region: frankfurt  # Close to EU region
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: false
    envVars:
      - key: PYTHON_VERSION
        value: 3.8
      - key: SECRET
        value: JWYNULQL35NGEO4QLYE24W6IN2TZJYNW  # Move to Render Dashboard for security
